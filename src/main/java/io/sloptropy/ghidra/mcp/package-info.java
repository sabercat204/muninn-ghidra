/**
 * Root package for the ghidra-mcp Ghidra extension.
 *
 * Sub-packages:
 *   io.sloptropy.ghidra.mcp.api      - McpTool interface and shared types
 *   io.sloptropy.ghidra.mcp.server   - Jetty + MCP SDK transport wiring
 *   io.sloptropy.ghidra.mcp.tools    - tool implementations (one class per spec)
 */
package io.sloptropy.ghidra.mcp;
